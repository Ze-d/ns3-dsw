画以下三张图，它们能从“因”、“果”和“统计规律”三个维度展示高bust和低bust节点的区别：

### 1\. 流量生成脉冲图 (Traffic Generation Impulse Plot) —— 展示“因”

这是最直观能一眼看出“平稳流”和“突发流”区别的图。

  * **X 轴**: 时间 (Time, seconds)
  * **Y 轴**: 瞬间生成的任务数量 (Number of Packets Generated)
  * **图表形式**: **Stem Plot (火柴杆图)** 或 **Step Plot (阶梯图)**。
  * **预期效果**:
      * **推理节点**: 像一片低矮浓密的“草丛”。你会看到密密麻麻的竖线，高度几乎全是 1，偶尔有 2，间隔非常均匀且紧凑。
      * **训练节点**: 像稀疏分布的“大树”。大部分时间是空的（0），然后突然出现一根很高的竖线（高度 8-15 左右），两根竖线之间间隔很久。

### 2\. 生产者积压演变图 (Producer Backlog Evolution) —— 展示“果”

这张图用来展示突发流量对调度器造成的瞬间压力，也是验证你调节 `producerSensitivity` 参数效果的关键图。

  * **X 轴**: 时间 (Time, seconds)
  * **Y 轴**: 待处理队列长度 (Pending Tasks in Queue)
  * **图表形式**: **Line Plot (折线图)**，建议使用填充颜色的面积图 (Area Plot)。
  * **预期效果**:
      * **推理节点**: 一条紧贴着 0 的直线，偶尔微微抖动到 1 或 2，然后迅速归零。说明任务一来就被调度走了。
      * **训练节点**: **锯齿波 (Sawtooth Wave)**。你会看到线条突然垂直拉升到 10 左右（突发到达），然后随着调度器分发任务，以斜线形式缓慢下降归零。如果你的调度器处理得不够快，你甚至会看到“波峰叠加”。

### 3\. 任务间隔累积分布图 (Inter-arrival CDF) —— 展示“统计规律”

如果你需要写论文或做学术报告，这张图是必不可少的，它证明了你的流量符合泊松/泊松簇分布。

  * **X 轴**: 任务到达间隔时间 (Inter-arrival Time, log scale)
  * **Y 轴**: 累积概率 (CDF, 0.0 - 1.0)
  * **图表形式**: **Step CDF**。
  * **预期效果**:
      * **推理节点**: 一条光滑的 S 形曲线（符合指数分布）。
      * **训练节点**: 可能会出现**双峰**或特殊的阶梯。因为簇内（Batch内）的间隔极短（几乎为0），而簇间（Batch间）的间隔很长。

### 总结建议

1.  先画 **图1 (Stem Plot)**：把两类节点的生成时刻画在上下两个子图中，强烈的视觉反差（密集的矮线 vs 稀疏的高线）能直接证明你的 Traffic Model 是成功的。
2.  再画 **图2 (Queue Backlog)**：这能证明引入突发流量的**必要性**——展示出训练节点的队列是如何瞬间积压，以及调度器是如何花时间“消化”这些积压的。这正是我们要研究调度策略的意义所在。